<div class="card">
    <div class="card-header" id="header-managers">
        <h2 class="mb-0">
            <button class="btn btn-link btn-block text-left" type="button" data-toggle="collapse" data-target="#collapse-managers" aria-expanded="false" aria-controls="collapse-managers">
                {{ 'List of administrators'|trans }} ({{ field.value|length }})
            </button>
        </h2>
    </div>
    <div id="collapse-managers" class="collapse" aria-labelledby="header-managers">
        <ul class="list-group list-group-flush">
            {% for client in field.value %}
                <li class="list-group-item">
                    {{ client.fullName }}
                </li>
            {% else %}
            <li class="list-group-item">
                {{ 'No administrator'|trans }}
            </li>
            {% endfor %}
        </ul>
    </div>
</div>
