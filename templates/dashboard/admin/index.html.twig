{% extends 'base.html.twig' %}

{% set pagetitle = 'Dashboard'|trans %}
{% block title pagetitle %}

{% block breadcrumb %}
	<div class="row">
		<div class="col-lg-12 col-md-12 col-12">
			<div class="border-bottom-0 pb-3 mb-3 d-lg-flex justify-content-between align-items-center">
				<div class="mb-3 mb-lg-0">
					<h1 class="mb-0 h2 fw-bold">{{ pagetitle }}</h1>
				</div>
				<div class="d-flex">
					<a href="{{ path('dashboard_user_index') }}" class="btn btn-primary">{{ 'My profile'|trans }}</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}

{% block body %}

{% endblock %}
