<form method="post" action="{{ path('dashboard_admin_product_delete', {'id': product.id}) }}" onsubmit="return confirm('{{ "Are you sure you want to delete this item?"|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('product_deletion_' ~ product.id) }}">
    <button class="btn btn-danger btn-sm">
        <i class="fas fa-trash fa-fw"></i>
        {{ "Delete"|trans }}
    </button>
</form>
