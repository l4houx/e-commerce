{% extends 'base.html.twig' %}

{% set pagetitle = 'Edit Profile'|trans %}
{% block title pagetitle %}

{% block body %}
    <section class="pt-sm-7">
        <div class="container pt-3 pt-xl-5">
            <div class="row">
                {% include "global/dashboard-items-navbar-vertical.html.twig" with {activerd: 'account_edit'} %}
                <div class="col-lg-8 col-xl-9 ps-lg-4 ps-xl-6">
                    <div class="d-flex justify-content-between align-items-center mb-5 mb-sm-6">
                        <h1 class="h3 mb-0">{{ 'My profile'|trans }}</h1>
                        <button class="btn btn-primary d-lg-none flex-shrink-0 ms-2" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasSidebar" aria-controls="offcanvasSidebar">
                            <i class="fas fa-sliders-h"></i> {{ 'Menu'|trans }}
                        </button>
                    </div>
                    <div class="card bg-transparent p-0">
                        <div class="card-header bg-transparent border-bottom p-0 pb-3">
                            <h6 class="mb-0">{{ 'Account status'|trans }}</h6>
                        </div>
                        <div class="card-body px-0">
                            <div class="row g-4">
                                <div class="col-12">
                                    <p class="mb-0">
                                        <span>{{ 'Account status'|trans }}</span>
                                        <span class="text-success fw-semibold ms-2">
                                            <i class="fas fa-user-check"></i> {{ "Enabled"|trans }}
                                        </span>
                                        <span class="ms-3">{{ "Last login"|trans }}</span> 
                                        <span class="text-success fw-semibold ms-3">
                                            <i class="fas fa-hand-point-right"></i> 
                                            {{ app.user.lastLogin|localizeddate('full', 'short', app.request.locale, date_timezone, date_format) }}
                                        </span>
                                        <span class="ms-3">{{ "Member since the"|trans }}</span>
                                        <span class="text-success fw-semibold ms-3">
                                            <i class="fas fa-user-plus"></i> 
                                            {{ app.user.createdAt|localizeddate('full', 'short', app.request.locale, date_timezone, date_format) }}
                                        </span>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="text-center my-5"><i class="fas fa-ellipsis"></i></div>
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'needs-validation'} }) }}
                        {{ form_widget(form._token) }}
                        {{ form_errors(form) }}
                        <div class="card bg-transparent p-0">
                            <div class="card-header bg-transparent border-bottom p-0 pb-3">
                                <h6 class="mb-0">{{ 'Personal Information'|trans }}</h6>
                            </div>
                            <div class="card-body px-0">
                                <div class="row g-4">
                                    <div class="col-12">
                                        {% component alert with {type: 'info', closeButton: false} %}
                                            {% block content %}
                                                {{ 'The fields preceded with a red * are mandatory'|trans }}
                                            {% endblock %}
                                        {% endcomponent %}
                                    </div>
                                    <div class="mb-3 col-12 col-md-4">
                                        {{ form_row(form.civility) }}
                                    </div>
                                    <div class="mb-3 col-12 col-md-4">
                                        {{ form_row(form.username) }}
                                    </div>
                                    <div class="mb-3 col-12 col-md-4">
                                        {{ form_row(form.firstname) }}
                                    </div>
                                    <div class="mb-3 col-12 col-md-4">
                                        {{ form_row(form.lastname) }}
                                    </div>
                                    <div class="mb-3 col-12 col-md-6">
                                        {{ form_row(form.email) }}
                                    </div>
                                    {% if form.phone is defined %}
                                        <div class="mb-3 col-12 col-md-6">
                                            {{ form_row(form.phone) }}
                                        </div>
                                    {% endif %}
                                    <div class="col-12 text-end">
                                        <button type="submit" class="btn btn-primary mb-0">{{ 'Apply changes'|trans }}</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {{ form_end(form, {'render_rest': false}) }}
                    <div class="text-center my-5"><i class="fas fa-ellipsis"></i></div>
                    <div class="card bg-transparent p-0">
                        <div class="card-header bg-transparent border-bottom px-0">
                            <h6 class="mb-0">{{ 'Email'|trans }}</h6>
                        </div>
                        <div class="card-body px-0">
                            <div class="mb-4">
                                <p class="mb-4">{{ 'Your current email address is'|trans }}
                                    <span class="text-primary">{{ user.email }}</span>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
