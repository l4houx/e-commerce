<a {% for attr, value  in options %} {{ attr }}="{{ value }}" {% endfor %}>
	{{ title }}
	{% if sorted %}
		{% if direction == 'desc' %}
			<i class="fas fa-caret-down"></i>
		{% else %}
			<i class="fas fa-caret-up"></i>
		{% endif %}
	{% endif %}
</a>
