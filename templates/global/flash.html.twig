<div data-turbo-temporary>
    {#  
    {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="container align-items-center justify-content-center mt-4">
                <div class="d-flex alert alert-{{ type }} alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="{{ 'Close'|trans }}"></button>
                </div>
            </div>
        {% endfor %}
    {% endfor %}
    #}

    {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="position-fixed bottom-0 end-0 p-3" style="z-index: 11">
                <div id="liveToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="toast-header text-white bg-{{ type }}">
                        <strong class="me-auto">{{ 'Notification'|trans }}</strong>
                        <small>{{ 'Now'|trans }}</small>
                        <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="{{ 'Close'|trans }}"></button>
                    </div>
                    <div class="toast-body">
                        {{ message }}
                    </div>
                </div>
            </div>
        {% endfor %}
    {% endfor %}
</div>